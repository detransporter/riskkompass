"""Loaders for public benchmark datasets (docs/FORECAST_SPEC.md Phase 10),
mapped onto forecasting/backtest.py's own (article_id, period, qty_ordered)
series shape so the exact same backtest/segmentation/FVA machinery Phases
2-4 already built runs unmodified against real data, not just
`datagen/v1`'s synthetic estate.
"""
