# Pinned to the same known-good range as iha-saas/requirements.txt -- see
# that file's comment for why (a pandas 3.0 / numba / statsmodels wheel
# incident on Streamlit Cloud forced these pins; carried over here even
# though this app deploys to a plain VPS, not Streamlit Cloud, since the
# underlying wheel-availability issue is not platform-specific).
streamlit>=1.35.0
pandas>=2.0.0,<3.0.0
numpy>=1.25.0,<2.2.0
openpyxl>=3.1.0  # engine for df.to_excel() -- Excel export buttons (components/export.py)

# sqlite3 is stdlib.
#
# Forecasting-specific dependencies (statsforecast, scipy, numba, statsmodels,
# and later lightgbm) live in requirements-forecast.txt, not here -- see that
# file's comment. Nothing under views/ imports analysis/demand_forecast.py or
# analysis/simulation.py yet (no forecast page exists), so the core app runs
# fine without it installed; keep it that way as forecasting/ grows, per
# docs/FORECAST_SPEC.md's "degrade gracefully" guardrail.
